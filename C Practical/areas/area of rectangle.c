#include<stdio.h>
#include<conio.h>

void main()
{
	//area of rectangle r=a*b
	int r,a,b;
	printf("enter length:");
	scanf("%d",&a);
	printf("enter width:");
	scanf("%d",&b);
	r=a*b;
	printf("area of rectangle is %d",r);
}
